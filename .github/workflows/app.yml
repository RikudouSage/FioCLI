name: Build app

on:
  push:
    branches:
      - "**"
    tags:
      - "**"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Check out repository
        uses: actions/checkout@v6
        with:
          submodules: recursive

      - name: Install Nix
        uses: cachix/install-nix-action@v31

      - name: Build app
        run: |
          nix develop --command make
          nix develop --command patchelf --set-interpreter /lib64/ld-linux-x86-64.so.2 fio
          nix develop --command patchelf --remove-rpath fio
          mkdir -p dist
          cp --dereference fio dist/fio-linux-amd64
          chmod 0755 dist/fio-linux-amd64

      - name: Upload workflow artifact
        uses: actions/upload-artifact@v7
        with:
          name: fio-linux-amd64
          path: dist/fio-linux-amd64
          archive: false

      - name: Create GitHub Release
        if: startsWith(github.ref, 'refs/tags/')
        uses: softprops/action-gh-release@v3
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          name: ${{ github.ref_name }}
          draft: false
          prerelease: false
          generate_release_notes: true
          files: dist/fio-linux-amd64
